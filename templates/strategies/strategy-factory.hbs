import { Injectable } from '@nestjs/common';
{{#each module.strategies}}
import { {{className}} } from './{{nameLower}}.strategy';
{{/each}}

@Injectable()
export class {{module.name}}StrategyFactory {
  constructor(
    {{#each module.strategies}}
    private readonly {{nameLower}}: {{className}},
    {{/each}}
  ) {}

  getStrategy(type: string): {{module.nameLower}}Strategy {
    switch (type) {
      {{#each module.strategies}}
      case '{{name}}':
        return this.{{nameLower}};
      {{/each}}
      default:
        throw new Error(`未知的巡检类型: ${type}`);
    }
  }
}
